<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="controller.AdminDashboardController"
            prefWidth="980" prefHeight="640"
            style="-fx-background-color: #f0f2f8;">

    <!-- TOP BAR -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="14"
              style="-fx-background-color: linear-gradient(to right, #1a1a2e, #16213e);
                     -fx-padding: 14 24;
                     -fx-effect: dropshadow(gaussian, #00000055, 8, 0, 0, 3);">
            <Label text="⚙️" style="-fx-font-size: 22px;"/>
            <VBox spacing="0">
                <Label text="Library Nexus"
                       style="-fx-text-fill: white; -fx-font-size: 17px; -fx-font-weight: bold;"/>
                <Label text="Admin Panel"
                       style="-fx-text-fill: rgba(255,255,255,0.4); -fx-font-size: 10px;"/>
            </VBox>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="welcomeLabel"
                   style="-fx-text-fill: rgba(255,255,255,0.65); -fx-font-size: 12px;"/>
        </HBox>
    </top>

    <!-- SIDEBAR -->
    <left>
        <VBox prefWidth="200"
              style="-fx-background-color: #16213e; -fx-padding: 20 0;">

            <Label text="NAVIGATION"
                   style="-fx-text-fill: rgba(255,255,255,0.3);
                          -fx-font-size: 10px;
                          -fx-font-weight: bold;
                          -fx-padding: 0 20 12 20;
                          -fx-letter-spacing: 1.5;"/>

            <Region VBox.vgrow="ALWAYS"/>

            <Button text="🚪  Logout"
                    onAction="#handleLogout"
                    maxWidth="Infinity"
                    style="-fx-background-color: rgba(231,76,60,0.15);
                           -fx-text-fill: #e74c3c;
                           -fx-font-size: 13px;
                           -fx-font-weight: bold;
                           -fx-padding: 12 20;
                           -fx-cursor: hand;
                           -fx-background-radius: 0;
                           -fx-alignment: CENTER_LEFT;"/>
        </VBox>
    </left>

    <!-- MAIN CONTENT -->
    <center>
        <VBox alignment="TOP_CENTER" spacing="16">
            <TableView fx:id="userTableView" prefWidth="700" prefHeight="500">
                <columns>
                    <TableColumn fx:id="idColumn" text="ID" prefWidth="60" />
                    <TableColumn fx:id="usernameColumn" text="Username" prefWidth="200" />
                    <TableColumn fx:id="roleColumn" text="Role" prefWidth="120" />
                    <TableColumn fx:id="statusColumn" text="Status" prefWidth="120" />
                </columns>
            </TableView>
            <HBox alignment="CENTER" spacing="12">
                <Button fx:id="banUserButton" text="Ban User" disable="true" style="-fx-background-color: #f39c12; -fx-text-fill: white; -fx-font-weight: bold;" />
                <Button fx:id="deleteUserButton" text="Delete User" disable="true" style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-weight: bold;" />
            </HBox>
        </VBox>
    </center>

</BorderPane>